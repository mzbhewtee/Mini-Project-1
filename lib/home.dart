import 'package:flutter/material.dart';

// this is just a dummy page so that the welcome screen runs smoothly. Please remove it and replace it with the code you wrote

class Home extends StatelessWidget {
  const Home({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Text("Hie"));
  }
}
